DATABASE_URL="postgresql://canals:canals@localhost:5433/canals_orders?schema=public"

POSTGRES_USER=canals
POSTGRES_PASSWORD=canals
POSTGRES_DB=canals_orders
POSTGRES_PORT=5433

# CORS allowlist (comma-separated origins). Unset = allow any origin.
# CORS_ORIGIN=https://app.example.com
